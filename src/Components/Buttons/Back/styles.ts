import styled from 'styled-components';

export const Button = styled.div`
    position: fixed; 
    width: 100%;
    left: 1%;
    bottom: 96%;
    height: 20px;
    font-size: 4rem;
    z-index: 1;
    cursor: pointer;
    color: var(--amarelo);
`